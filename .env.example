# Database Connection (SQLite for local development; switchable to PostgreSQL)
DATABASE_URL="file:./dev.db"

# JWT Authentication Secret (Used for signing session tokens)
JWT_SECRET="super-secret-careerbridge-jwt-key-min-32-chars-ghana-2025"

# Application Base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment
NODE_ENV="development"
